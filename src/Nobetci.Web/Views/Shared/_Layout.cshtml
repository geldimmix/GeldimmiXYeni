@{
    var isTurkish = System.Globalization.CultureInfo.CurrentUICulture.TwoLetterISOLanguageName == "tr";
}
<!DOCTYPE html>
<html lang="@System.Globalization.CultureInfo.CurrentUICulture.TwoLetterISOLanguageName">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - Geldimmi</title>
    
    <!-- SEO Meta Tags -->
    @if (ViewData["MetaDescription"] != null)
    {
        <meta name="description" content="@ViewData["MetaDescription"]" />
    }
    @if (ViewData["MetaKeywords"] != null)
    {
        <meta name="keywords" content="@ViewData["MetaKeywords"]" />
    }
    <meta name="robots" content="index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1" />
    <meta name="author" content="Geldimmi" />
    <meta name="googlebot" content="index, follow" />
    <meta name="bingbot" content="index, follow" />
    <meta name="theme-color" content="#d4553d" />
    <meta name="application-name" content="Geldimmi" />
    <meta name="mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="default" />
    <meta name="apple-mobile-web-app-title" content="Geldimmi" />
    <link rel="canonical" href="https://geldimmi.com@Context.Request.Path" />
    
    <!-- Language Alternates (hreflang) -->
    <link rel="alternate" hreflang="tr" href="https://geldimmi.com@Context.Request.Path?culture=tr-TR" />
    <link rel="alternate" hreflang="en" href="https://geldimmi.com@Context.Request.Path?culture=en-US" />
    <link rel="alternate" hreflang="x-default" href="https://geldimmi.com@Context.Request.Path" />
    
    <!-- Open Graph / Facebook -->
    <meta property="og:site_name" content="Geldimmi" />
    <meta property="og:title" content="@ViewData["Title"] - Geldimmi" />
    <meta property="og:description" content="@(ViewData["MetaDescription"] ?? (isTurkish ? "Online nöbet çizelgesi, mesai takip ve puantaj sistemi" : "Online shift scheduling, time attendance and payroll system"))" />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="https://geldimmi.com@Context.Request.Path" />
    <meta property="og:image" content="https://geldimmi.com/images/og-image.png" />
    <meta property="og:locale" content="@(isTurkish ? "tr_TR" : "en_US")" />
    
    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="@ViewData["Title"] - Geldimmi" />
    <meta name="twitter:description" content="@(ViewData["MetaDescription"] ?? (isTurkish ? "Online nöbet çizelgesi, mesai takip ve puantaj sistemi" : "Online shift scheduling, time attendance and payroll system"))" />
    <meta name="twitter:image" content="https://geldimmi.com/images/og-image.png" />
    
    <!-- Structured Data (JSON-LD) -->
    @await RenderSectionAsync("StructuredData", required: false)
    
    <!-- Favicon & App Icons -->
    <link rel="icon" type="image/x-icon" href="~/favicon.ico" />
    <link rel="icon" type="image/png" sizes="32x32" href="~/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="~/favicon-16x16.png" />
    <link rel="apple-touch-icon" sizes="180x180" href="~/apple-touch-icon.png" />
    <link rel="manifest" href="~/site.webmanifest" />
    
    <!-- Bootstrap 5 -->
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    
    <!-- Custom CSS -->
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:opsz,wght@9..40,400;9..40,500;9..40,600;9..40,700&family=Instrument+Serif:ital@0;1&display=swap" rel="stylesheet">
    
    <!-- Google Analytics 4 (GA4) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-4GE9SRXDNG"></script>
    <script>
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        gtag('js', new Date());
        gtag('config', 'G-4GE9SRXDNG', {
            'page_location': window.location.href,
            'page_path': window.location.pathname
        });
    </script>
    
    @await RenderSectionAsync("Styles", required: false)
</head>
<body>
    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg sticky-top navbar-custom">
        <div class="container">
            <a class="navbar-brand brand-link-main" href="~/">
                <div class="brand-icon-main">G</div>
                <span class="brand-name-main">Geldimmi</span>
            </a>
            
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav mx-auto nav-center">
                    <li class="nav-item">
                        <a class="nav-link" asp-controller="Home" asp-action="Index">
                            @(isTurkish ? "Ana Sayfa" : "Home")
                        </a>
                    </li>
                    
                    <!-- Products Dropdown -->
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="featuresDropdown" role="button" data-bs-toggle="dropdown">
                            @(isTurkish ? "Ürünler" : "Products")
                        </a>
                        <ul class="dropdown-menu dropdown-menu-custom dropdown-mega">
                            <div class="dropdown-mega-inner">
                                <div class="dropdown-column">
                                    <div class="dropdown-header">📅 @(isTurkish ? "Nöbet Çizelgesi" : "Shift Scheduling")</div>
                                    <li>
                                        <a class="dropdown-item" href="@(isTurkish ? "/rehber/nobet-listesi-olusturma" : "/guide/shift-scheduling")">
                                            <span class="dropdown-text">
                                                <strong>@(isTurkish ? "Nöbet Çizelgesi Oluşturma" : "Create Shift Schedule")</strong>
                                                <small>@(isTurkish ? "Dakikalar içinde profesyonel nöbet planı" : "Professional shift planning in minutes")</small>
                                            </span>
                                            <span class="dropdown-arrow">→</span>
                                        </a>
                                    </li>
                                    <li>
                                        <a class="dropdown-item" href="@(isTurkish ? "/rehber/hemsire-nobet-programi" : "/guide/nurse-shift-planner")">
                                            <span class="dropdown-text">
                                                <strong>@(isTurkish ? "Hemşire Nöbet Programı" : "Nurse Shift Planner")</strong>
                                                <small>@(isTurkish ? "Hastaneler için özel çözüm" : "Special solution for hospitals")</small>
                                            </span>
                                            <span class="dropdown-arrow">→</span>
                                        </a>
                                    </li>
                                    <li>
                                        <a class="dropdown-item" href="@(isTurkish ? "/rehber/adil-nobet-dagitimi" : "/guide/fair-shift-distribution")">
                                            <span class="dropdown-text">
                                                <strong>@(isTurkish ? "Adil Nöbet Dağıtımı" : "Fair Shift Distribution")</strong>
                                                <small>@(isTurkish ? "Akıllı algoritma ile dengeli dağıtım" : "Balanced distribution with smart algorithm")</small>
                                            </span>
                                            <span class="dropdown-arrow">→</span>
                                        </a>
                                    </li>
                                </div>
                                <div class="dropdown-column">
                                    <div class="dropdown-header">🕐 @(isTurkish ? "Mesai Takip" : "Time Attendance")</div>
                                    <li>
                                        <a class="dropdown-item" href="@(isTurkish ? "/rehber/mesai-takip-sistemi" : "/guide/time-attendance-system")">
                                            <span class="dropdown-text">
                                                <strong>@(isTurkish ? "Mesai Takip Sistemi" : "Time Attendance System")</strong>
                                                <small>@(isTurkish ? "Giriş-çıkış saatlerini kaydedin" : "Record check-in/check-out times")</small>
                                            </span>
                                            <span class="dropdown-arrow">→</span>
                                        </a>
                                    </li>
                                    <li>
                                        <a class="dropdown-item" href="/app/attendance">
                                            <span class="dropdown-text">
                                                <strong>@(isTurkish ? "Mesai Takip Uygulaması" : "Attendance App")</strong>
                                                <small>@(isTurkish ? "Hemen kullanmaya başla" : "Start using now")</small>
                                            </span>
                                            <span class="dropdown-arrow">→</span>
                                        </a>
                                    </li>
                                </div>
                                <div class="dropdown-column">
                                    <div class="dropdown-header">📊 @(isTurkish ? "Puantaj" : "Payroll")</div>
                                    <li>
                                        <a class="dropdown-item" href="@(isTurkish ? "/rehber/puantaj-hesaplama" : "/guide/timesheet-calculation")">
                                            <span class="dropdown-text">
                                                <strong>@(isTurkish ? "Puantaj Hesaplama" : "Payroll Calculation")</strong>
                                                <small>@(isTurkish ? "Otomatik mesai hesabı" : "Automatic work hour calculation")</small>
                                            </span>
                                            <span class="dropdown-arrow">→</span>
                                        </a>
                                    </li>
                                    <li>
                                        <a class="dropdown-item" href="@(isTurkish ? "/rehber/fazla-mesai-hesaplama" : "/guide/overtime-calculation")">
                                            <span class="dropdown-text">
                                                <strong>@(isTurkish ? "Fazla Mesai Hesaplama" : "Overtime Calculation")</strong>
                                                <small>@(isTurkish ? "Gece, hafta sonu, tatil" : "Night, weekend, holiday")</small>
                                            </span>
                                            <span class="dropdown-arrow">→</span>
                                        </a>
                                    </li>
                                </div>
                                <div class="dropdown-column">
                                    <div class="dropdown-header">🏖️ @(isTurkish ? "İzin Yönetimi" : "Leave Management")</div>
                                    <li>
                                        <a class="dropdown-item" href="@(isTurkish ? "/rehber/izin-yonetimi" : "/guide/leave-management")">
                                            <span class="dropdown-text">
                                                <strong>@(isTurkish ? "İzin Yönetim Sistemi" : "Leave Management System")</strong>
                                                <small>@(isTurkish ? "30+ izin türü desteği" : "30+ leave type support")</small>
                                            </span>
                                            <span class="dropdown-arrow">→</span>
                                        </a>
                                    </li>
                                    <li>
                                        <a class="dropdown-item" href="@(isTurkish ? "/rehber/yillik-izin-takibi" : "/guide/annual-leave-tracking")">
                                            <span class="dropdown-text">
                                                <strong>@(isTurkish ? "Yıllık İzin Takibi" : "Annual Leave Tracking")</strong>
                                                <small>@(isTurkish ? "Bakiye ve kullanım takibi" : "Balance and usage tracking")</small>
                                            </span>
                                            <span class="dropdown-arrow">→</span>
                                        </a>
                                    </li>
                                </div>
                            </div>
                        </ul>
                    </li>
                    
                    <li class="nav-item">
                        <a class="nav-link" href="/blog">Blog</a>
                    </li>
                    
                    <li class="nav-item">
                        <a class="nav-link nav-link-cta" asp-controller="App" asp-action="Index">
                            @(isTurkish ? "Ücretsiz Başla" : "Start Free")
                        </a>
                    </li>
                </ul>
                
                <ul class="navbar-nav nav-actions">
                    <!-- Language Selector -->
                    <li class="nav-item dropdown">
                        <a class="nav-link lang-toggle" href="#" id="languageDropdown" role="button" data-bs-toggle="dropdown">
                            @if (isTurkish)
                            {
                                <span>TR</span>
                            }
                            else
                            {
                                <span>EN</span>
                            }
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end dropdown-menu-custom dropdown-lang">
                            <li>
                                <form asp-controller="Home" asp-action="SetLanguage" method="post">
                                    <input type="hidden" name="culture" value="tr" />
                                    <input type="hidden" name="returnUrl" value="@Context.Request.Path" />
                                    <button type="submit" class="dropdown-item @(isTurkish ? "active" : "")">
                                        Türkçe
                                    </button>
                                </form>
                            </li>
                            <li>
                                <form asp-controller="Home" asp-action="SetLanguage" method="post">
                                    <input type="hidden" name="culture" value="en" />
                                    <input type="hidden" name="returnUrl" value="@Context.Request.Path" />
                                    <button type="submit" class="dropdown-item @(!isTurkish ? "active" : "")">
                                        English
                                    </button>
                                </form>
                            </li>
                        </ul>
                    </li>
                    
                    @if (User.Identity?.IsAuthenticated == true)
                    {
                        <li class="nav-item">
                            <a class="nav-link" asp-controller="Account" asp-action="Logout">
                                @(isTurkish ? "Çıkış" : "Logout")
                            </a>
                        </li>
                    }
                    else
                    {
                        <li class="nav-item d-none d-lg-block">
                            <a class="nav-link" asp-controller="Account" asp-action="Login">
                                @(isTurkish ? "Giriş" : "Login")
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="btn btn-nav-signup" asp-controller="Account" asp-action="Register">
                                @(isTurkish ? "Kayıt Ol" : "Sign Up")
                            </a>
                        </li>
                    }
                </ul>
            </div>
        </div>
    </nav>
    
    <style>
        /* ═══════════════════════════════════════════════════════════════
           MINIMAL NAVBAR - Clean & Elegant
           ═══════════════════════════════════════════════════════════════ */
        
        :root {
            --nav-bg: #fefefe;
            --nav-text: #2d2d2d;
            --nav-text-muted: #737373;
            --nav-accent: #c94a4a;
            --nav-border: #ebebeb;
            --nav-hover-bg: #f5f5f5;
        }
        
        .navbar-custom {
            background: var(--nav-bg);
            border-bottom: 1px solid var(--nav-border);
            padding: 0;
            font-family: 'DM Sans', -apple-system, BlinkMacSystemFont, sans-serif;
        }
        
        .navbar-custom .container {
            padding-top: 16px;
            padding-bottom: 16px;
        }
        
        .navbar-custom.scrolled {
            box-shadow: 0 1px 0 var(--nav-border);
        }
        
        /* Brand */
        .brand-link-main {
            display: flex;
            align-items: center;
            gap: 10px;
            text-decoration: none;
        }
        
        .brand-icon-main {
            width: 36px;
            height: 36px;
            background: var(--nav-accent);
            border-radius: 8px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Instrument Serif', Georgia, serif;
            font-size: 20px;
            font-style: italic;
            color: white;
            transition: transform 0.2s ease;
        }
        
        .brand-name-main {
            font-family: 'Instrument Serif', Georgia, serif;
            font-size: 1.5rem;
            font-weight: 400;
            font-style: italic;
            letter-spacing: -0.02em;
            color: var(--nav-text);
        }
        
        .brand-link-main:hover .brand-icon-main {
            transform: rotate(-4deg);
        }
        
        /* Nav Links */
        .nav-center {
            gap: 0;
        }
        
        .navbar-custom .nav-link {
            color: var(--nav-text-muted);
            font-size: 0.95rem;
            font-weight: 500;
            padding: 8px 20px;
            position: relative;
            transition: color 0.2s ease;
        }
        
        .navbar-custom .nav-link::after {
            content: '';
            position: absolute;
            bottom: 4px;
            left: 20px;
            right: 20px;
            height: 1px;
            background: var(--nav-accent);
            transform: scaleX(0);
            transition: transform 0.2s ease;
        }
        
        .navbar-custom .nav-link:hover {
            color: var(--nav-text);
        }
        
        .navbar-custom .nav-link:hover::after {
            transform: scaleX(1);
        }
        
        /* CTA Link */
        .nav-link-cta {
            color: var(--nav-accent) !important;
            font-weight: 600 !important;
        }
        
        .nav-link-cta::after {
            display: none !important;
        }
        
        .nav-link-cta:hover {
            color: #a33d3d !important;
        }
        
        /* Actions */
        .nav-actions {
            display: flex;
            align-items: center;
            gap: 8px;
        }
        
        /* Language Toggle */
        .lang-toggle {
            font-size: 0.85rem !important;
            font-weight: 600 !important;
            color: var(--nav-text-muted) !important;
            padding: 6px 12px !important;
            border: 1px solid var(--nav-border);
            border-radius: 6px;
        }
        
        .lang-toggle:hover {
            background: var(--nav-hover-bg);
            color: var(--nav-text) !important;
        }
        
        .lang-toggle::after {
            display: none !important;
        }
        
        /* Signup Button */
        .btn-nav-signup {
            padding: 10px 22px;
            font-size: 0.9rem;
            font-weight: 600;
            border-radius: 8px;
            background: var(--nav-text);
            border: none;
            color: white;
            transition: all 0.2s ease;
        }
        
        .btn-nav-signup:hover {
            background: #1a1a1a;
            color: white;
            transform: translateY(-1px);
        }
        
        /* ═══════════════════════════════════════════════════════════════
           DROPDOWN STYLES
           ═══════════════════════════════════════════════════════════════ */
        
        .dropdown-menu-custom {
            border: 1px solid var(--nav-border);
            box-shadow: 0 16px 48px rgba(0, 0, 0, 0.08);
            border-radius: 12px;
            padding: 0;
            overflow: hidden;
            margin-top: 12px;
            animation: dropdownFade 0.2s ease;
        }
        
        @@keyframes dropdownFade {
            from { opacity: 0; transform: translateY(-4px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .dropdown-mega {
            min-width: 900px;
            background: white;
            left: 50%;
            transform: translateX(-50%);
        }
        
        .dropdown-mega-inner {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
        }
        
        .dropdown-column {
            padding: 20px;
            border-right: 1px solid var(--nav-border);
        }
        
        .dropdown-column:last-child {
            border-right: none;
        }
        
        .dropdown-menu-custom .dropdown-header {
            font-size: 0.7rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1.2px;
            color: var(--nav-text-muted);
            padding: 0 0 16px 0;
            margin-bottom: 0;
        }
        
        .dropdown-menu-custom .dropdown-item {
            display: flex;
            align-items: center;
            justify-content: space-between;
            padding: 14px 0;
            border-radius: 0;
            border-bottom: 1px solid #f5f5f5;
            transition: all 0.15s ease;
        }
        
        .dropdown-menu-custom .dropdown-item:last-child {
            border-bottom: none;
        }
        
        .dropdown-menu-custom .dropdown-item:hover {
            background: transparent;
            padding-left: 8px;
        }
        
        .dropdown-text {
            display: flex;
            flex-direction: column;
            gap: 2px;
        }
        
        .dropdown-text strong {
            font-size: 0.9rem;
            font-weight: 600;
            color: var(--nav-text);
        }
        
        .dropdown-text small {
            font-size: 0.8rem;
            color: var(--nav-text-muted);
        }
        
        .dropdown-arrow {
            color: var(--nav-text-muted);
            font-size: 1rem;
            opacity: 0;
            transform: translateX(-4px);
            transition: all 0.15s ease;
        }
        
        .dropdown-item:hover .dropdown-arrow {
            opacity: 1;
            transform: translateX(0);
            color: var(--nav-accent);
        }
        
        .dropdown-item:hover .dropdown-text strong {
            color: var(--nav-accent);
        }
        
        /* Language Dropdown */
        .dropdown-lang {
            min-width: 140px;
            padding: 8px;
            background: white;
        }
        
        .dropdown-lang .dropdown-item {
            padding: 10px 14px;
            border-radius: 6px;
            border-bottom: none;
            font-weight: 500;
            font-size: 0.9rem;
            color: var(--nav-text-muted);
        }
        
        .dropdown-lang .dropdown-item:hover {
            background: var(--nav-hover-bg);
            padding-left: 14px;
            color: var(--nav-text);
        }
        
        .dropdown-lang .dropdown-item.active {
            background: var(--nav-hover-bg);
            color: var(--nav-text);
        }
        
        /* ═══════════════════════════════════════════════════════════════
           RESPONSIVE
           ═══════════════════════════════════════════════════════════════ */
        
        @@media (max-width: 991.98px) {
            .nav-center {
                padding: 20px 0;
                border-top: 1px solid var(--nav-border);
                margin-top: 16px;
            }
            
            .navbar-custom .nav-link {
                padding: 12px 0;
            }
            
            .navbar-custom .nav-link::after {
                display: none;
            }
            
            .nav-actions {
                flex-direction: column;
                width: 100%;
                padding-top: 16px;
                border-top: 1px solid var(--nav-border);
                gap: 12px;
            }
            
            .dropdown-mega {
                min-width: 100%;
                border: none;
                box-shadow: none;
                margin-top: 0;
                left: 0;
                transform: none;
            }
            
            .dropdown-mega-inner {
                grid-template-columns: 1fr;
            }
            
            .dropdown-column {
                border-right: none;
                border-bottom: 1px solid var(--nav-border);
            }
            
            .dropdown-column:last-child {
                border-bottom: none;
            }
            
            .lang-toggle,
            .btn-nav-signup {
                width: 100%;
                justify-content: center;
                text-align: center;
            }
        }
        
        /* Navbar Toggler */
        .navbar-toggler {
            border: 1px solid var(--nav-border);
            padding: 6px 10px;
            border-radius: 6px;
        }
        
        .navbar-toggler:focus {
            box-shadow: none;
        }
    </style>
    
    <script>
        window.addEventListener('scroll', function() {
            const navbar = document.querySelector('.navbar-custom');
            if (window.scrollY > 10) {
                navbar.classList.add('scrolled');
            } else {
                navbar.classList.remove('scrolled');
            }
        });
    </script>

    <!-- Main Content -->
    <main>
        @RenderBody()
    </main>

    <!-- Footer -->
    <footer class="footer-section">
        <div class="container">
            <div class="footer-main">
                <div class="row g-5">
                    <!-- Brand -->
                    <div class="col-lg-4 col-md-6">
                        <div class="footer-brand">
                            <a href="~/" class="d-inline-flex align-items-center text-decoration-none mb-3 gap-2">
                                <div class="footer-brand-icon">G</div>
                                <span class="footer-brand-text">Geldimmi</span>
                            </a>
                            <p class="footer-desc">
                                @(isTurkish 
                                    ? "Hastaneler ve işletmeler için akıllı nöbet planlama ve otomatik puantaj sistemi."
                                    : "Smart shift planning and automatic timesheet system for hospitals and businesses.")
                            </p>
                        </div>
                    </div>
                    
                    <!-- Quick Links -->
                    <div class="col-lg-2 col-md-6 col-6">
                        <h6 class="footer-title">@(isTurkish ? "Ürün" : "Product")</h6>
                        <ul class="footer-links">
                            <li><a href="/app">@(isTurkish ? "Nöbet Oluştur" : "Create Shift")</a></li>
                            <li><a href="/app">@(isTurkish ? "Puantaj Al" : "Get Timesheet")</a></li>
                        </ul>
                    </div>
                    
                    <!-- Resources -->
                    <div class="col-lg-2 col-md-6 col-6">
                        <h6 class="footer-title">@(isTurkish ? "Kaynaklar" : "Resources")</h6>
                        <ul class="footer-links">
                            <li><a href="/blog">Blog</a></li>
                            <li><a href="/seo/nobet-listesi-olusturma">@(isTurkish ? "Nöbet Rehberi" : "Shift Guide")</a></li>
                            <li><a href="/seo/puantaj-hesaplama">@(isTurkish ? "Puantaj Rehberi" : "Timesheet Guide")</a></li>
                        </ul>
                    </div>
                    
                    <!-- Legal -->
                    <div class="col-lg-2 col-md-6 col-6">
                        <h6 class="footer-title">@(isTurkish ? "Yasal" : "Legal")</h6>
                        <ul class="footer-links">
                            <li><a href="/privacy">@(isTurkish ? "Gizlilik" : "Privacy")</a></li>
                            <li><a href="/terms">@(isTurkish ? "Şartlar" : "Terms")</a></li>
                            <li><a href="/sitemap.xml" target="_blank">Sitemap</a></li>
                        </ul>
                    </div>
                    
                    <!-- Contact -->
                    <div class="col-lg-2 col-md-6 col-6">
                        <h6 class="footer-title">@(isTurkish ? "İletişim" : "Contact")</h6>
                        <ul class="footer-links">
                            <li><a href="mailto:info@geldimmi.com">info&#64;geldimmi.com</a></li>
                        </ul>
                    </div>
                </div>
            </div>
            
            <div class="footer-bottom">
                <div class="row align-items-center">
                    <div class="col-md-6 text-center text-md-start">
                        <p class="mb-0">© 2026 Geldimmi. @(isTurkish ? "Tüm hakları saklıdır." : "All rights reserved.")</p>
                    </div>
                    <div class="col-md-6 text-center text-md-end mt-2 mt-md-0">
                        <span class="footer-made">
                            @(isTurkish ? "Türkiye'de yapıldı" : "Made in Turkey")
                        </span>
                    </div>
                </div>
            </div>
        </div>
    </footer>
    
    <style>
        .footer-section {
            background: #fafafa;
            border-top: 1px solid var(--nav-border);
            margin-top: 0;
            font-family: 'DM Sans', -apple-system, BlinkMacSystemFont, sans-serif;
        }
        
        .footer-main {
            padding: 64px 0 48px;
        }
        
        .footer-brand-icon {
            width: 32px;
            height: 32px;
            background: var(--nav-accent);
            border-radius: 6px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-family: 'Instrument Serif', Georgia, serif;
            font-size: 16px;
            font-style: italic;
            color: white;
        }
        
        .footer-brand-text {
            font-family: 'Instrument Serif', Georgia, serif;
            font-size: 1.3rem;
            font-weight: 400;
            font-style: italic;
            letter-spacing: -0.02em;
            color: var(--nav-text);
        }
        
        .footer-desc {
            color: var(--nav-text-muted);
            font-size: 0.9rem;
            line-height: 1.7;
            max-width: 300px;
        }
        
        .footer-title {
            font-size: 0.7rem;
            font-weight: 700;
            text-transform: uppercase;
            letter-spacing: 1.2px;
            color: var(--nav-text-muted);
            margin-bottom: 20px;
        }
        
        .footer-links {
            list-style: none;
            padding: 0;
            margin: 0;
        }
        
        .footer-links li {
            margin-bottom: 12px;
        }
        
        .footer-links a {
            color: var(--nav-text);
            text-decoration: none;
            font-size: 0.9rem;
            font-weight: 500;
            transition: color 0.2s ease;
        }
        
        .footer-links a:hover {
            color: var(--nav-accent);
        }
        
        .footer-bottom {
            padding: 24px 0;
            border-top: 1px solid var(--nav-border);
        }
        
        .footer-bottom p {
            color: var(--nav-text-muted);
            font-size: 0.85rem;
        }
        
        .footer-made {
            color: var(--nav-text-muted);
            font-size: 0.85rem;
        }
    </style>

    <!-- Scripts -->
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
