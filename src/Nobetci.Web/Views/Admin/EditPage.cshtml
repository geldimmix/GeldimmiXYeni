@model Nobetci.Web.Data.Entities.ContentPage
@{
    ViewData["Title"] = "Sayfa Düzenle";
    Layout = "_AdminLayout";
}

<div class="admin-header">
    <h2>
        <span>📝</span>
        Sayfa Düzenle
    </h2>
    <div class="header-actions">
        <a href="/@(Model.Language == "tr" ? "rehber" : "guide")/@Model.Slug" target="_blank" class="btn btn-secondary">
            <span>👁️</span>
            Önizle
        </a>
        <a href="/admin/pages" class="btn btn-secondary">
            <span>←</span>
            Geri Dön
        </a>
    </div>
</div>

<form method="post">
    <input type="hidden" name="Id" value="@Model.Id" />
    <input type="hidden" name="CreatedAt" value="@Model.CreatedAt.ToString("o")" />
    
    <div class="grid-2">
        <div class="card">
            <div class="card-header">
                <span class="card-title">
                    <span>📝</span>
                    Temel Bilgiler
                </span>
            </div>
            
            <div class="form-group">
                <label class="form-label">Başlık *</label>
                <input type="text" name="Title" class="form-control" required value="@Model.Title">
            </div>
            
            <div class="form-group">
                <label class="form-label">URL Slug *</label>
                <input type="text" name="Slug" class="form-control" required value="@Model.Slug">
                <small style="color: var(--text-muted); font-size: 0.8rem; margin-top: 6px; display: block;">
                    URL'de görünecek kısım (küçük harf, tire ile)
                </small>
            </div>
            
            <div class="form-group">
                <label class="form-label">Dil *</label>
                <select name="Language" class="form-control">
                    @if (Model.Language == "tr")
                    {
                        <option value="tr" selected>🇹🇷 Türkçe</option>
                        <option value="en">🇬🇧 İngilizce</option>
                    }
                    else
                    {
                        <option value="tr">🇹🇷 Türkçe</option>
                        <option value="en" selected>🇬🇧 İngilizce</option>
                    }
                </select>
            </div>
            
            <div class="form-group">
                <label class="form-label">Sayfa Türü</label>
                <select name="PageType" class="form-control">
                    <option value="1" selected="@(Model.PageType == Nobetci.Web.Data.Entities.PageType.Feature)">⭐ Özellik Sayfası</option>
                    <option value="2" selected="@(Model.PageType == Nobetci.Web.Data.Entities.PageType.Blog)">📝 Blog Yazısı</option>
                    <option value="3" selected="@(Model.PageType == Nobetci.Web.Data.Entities.PageType.Legal)">📜 Yasal Sayfa</option>
                    <option value="0" selected="@(Model.PageType == Nobetci.Web.Data.Entities.PageType.Landing)">🏠 Landing Page</option>
                </select>
            </div>
            
            <div class="form-group">
                <label class="form-label">Alt Başlık</label>
                <input type="text" name="Subtitle" class="form-control" value="@Model.Subtitle">
            </div>
            
            <div class="form-group">
                <label class="form-label">Sıra No</label>
                <input type="number" name="DisplayOrder" class="form-control" value="@Model.DisplayOrder">
            </div>
            
            <div class="form-group">
                <label class="form-check" style="cursor: pointer; padding: 14px 18px; border-radius: var(--radius-md); border: 2px solid @(Model.IsPublished ? "var(--success)" : "var(--border-color)"); @(Model.IsPublished ? "background: rgba(16, 185, 129, 0.05);" : "")">
                    <input type="checkbox" name="IsPublished" value="true" @(Model.IsPublished ? "checked" : "") style="width: 20px; height: 20px; accent-color: var(--success);">
                    <span style="font-weight: 600;">@(Model.IsPublished ? "✅ Yayında" : "Yayınla")</span>
                </label>
            </div>
        </div>
        
        <div class="card">
            <div class="card-header">
                <span class="card-title">
                    <span>🔍</span>
                    SEO Ayarları
                </span>
            </div>
            
            <div class="form-group">
                <label class="form-label">Meta Açıklama</label>
                <textarea name="MetaDescription" class="form-control" rows="3">@Model.MetaDescription</textarea>
            </div>
            
            <div class="form-group">
                <label class="form-label">Meta Anahtar Kelimeler</label>
                <input type="text" name="MetaKeywords" class="form-control" value="@Model.MetaKeywords">
            </div>
            
            <hr style="margin: 24px 0; border: none; border-top: 2px solid var(--border-color);">
            
            <div class="form-group">
                <label class="form-label">CTA Buton Metni</label>
                <input type="text" name="CtaText" class="form-control" value="@Model.CtaText">
            </div>
            
            <div class="form-group">
                <label class="form-label">CTA Buton URL</label>
                <input type="text" name="CtaUrl" class="form-control" value="@Model.CtaUrl">
            </div>
        </div>
    </div>
    
    <div class="card">
        <div class="card-header">
            <span class="card-title">
                <span>📖</span>
                Sayfa İçeriği (HTML)
            </span>
        </div>
        
        <div class="form-group" style="margin-bottom: 0;">
            <textarea name="Content" class="form-control" rows="20" style="font-family: 'SF Mono', Monaco, monospace; font-size: 0.9rem;">@Model.Content</textarea>
            <small style="color: var(--text-muted); font-size: 0.8rem; margin-top: 8px; display: block;">
                💡 HTML etiketleri kullanabilirsiniz: &lt;h2&gt;, &lt;p&gt;, &lt;ul&gt;, &lt;li&gt;, &lt;strong&gt; vb.
            </small>
        </div>
    </div>
    
    <div style="display: flex; gap: 12px; margin-top: 24px;">
        <button type="submit" class="btn btn-primary btn-lg">
            <span>💾</span>
            Güncelle
        </button>
        <a href="/admin/pages" class="btn btn-secondary btn-lg">İptal</a>
    </div>
</form>

<div class="card" style="margin-top: 24px; background: var(--bg-main);">
    <div style="display: flex; gap: 24px; color: var(--text-muted); font-size: 0.85rem;">
        <div>
            <span style="font-weight: 600; color: var(--text-secondary);">Oluşturulma:</span>
            @Model.CreatedAt.ToString("dd.MM.yyyy HH:mm")
        </div>
        <div>
            <span style="font-weight: 600; color: var(--text-secondary);">Son Güncelleme:</span>
            @Model.UpdatedAt.ToString("dd.MM.yyyy HH:mm")
        </div>
    </div>
</div>
