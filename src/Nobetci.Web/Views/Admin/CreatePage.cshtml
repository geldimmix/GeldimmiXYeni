@model Nobetci.Web.Data.Entities.ContentPage
@{
    ViewData["Title"] = "Yeni Sayfa";
    Layout = "_AdminLayout";
}

<div class="admin-header">
    <h2>
        <span>📄</span>
        Yeni Sayfa Oluştur
    </h2>
    <div class="header-actions">
        <a href="/admin/pages" class="btn btn-secondary">
            <span>←</span>
            Geri Dön
        </a>
    </div>
</div>

<form method="post">
    <div class="grid-2">
        <div class="card">
            <div class="card-header">
                <span class="card-title">
                    <span>📝</span>
                    Temel Bilgiler
                </span>
            </div>
            
            <div class="form-group">
                <label class="form-label">Başlık *</label>
                <input type="text" name="Title" class="form-control" required placeholder="Örn: Nöbet Listesi Oluşturma">
            </div>
            
            <div class="form-group">
                <label class="form-label">URL Slug *</label>
                <input type="text" name="Slug" class="form-control" required placeholder="Örn: nobet-listesi-olusturma">
                <small style="color: var(--text-muted); font-size: 0.8rem; margin-top: 6px; display: block;">
                    URL'de görünecek kısım (küçük harf, tire ile)
                </small>
            </div>
            
            <div class="form-group">
                <label class="form-label">Dil *</label>
                <select name="Language" class="form-control">
                    <option value="tr">🇹🇷 @Localizer["Turkish"]</option>
                    <option value="en">🇬🇧 @Localizer["English"]</option>
                </select>
            </div>
            
            <div class="form-group">
                <label class="form-label">Sayfa Türü</label>
                <select name="PageType" class="form-control">
                    <option value="1">⭐ Özellik Sayfası</option>
                    <option value="2">📝 Blog Yazısı</option>
                    <option value="3">📜 Yasal Sayfa</option>
                    <option value="0">🏠 Landing Page</option>
                </select>
            </div>
            
            <div class="form-group">
                <label class="form-label">Alt Başlık</label>
                <input type="text" name="Subtitle" class="form-control" placeholder="Hero bölümünde görünecek alt başlık">
            </div>
            
            <div class="form-group">
                <label class="form-label">Sıra No</label>
                <input type="number" name="DisplayOrder" class="form-control" value="0">
            </div>
            
            <div class="form-group">
                <label class="form-check" style="cursor: pointer; padding: 14px 18px; border-radius: var(--radius-md); border: 2px solid var(--success); background: rgba(16, 185, 129, 0.05);">
                    <input type="checkbox" name="IsPublished" value="true" checked style="width: 20px; height: 20px; accent-color: var(--success);">
                    <span style="font-weight: 600;">✅ Yayınla</span>
                </label>
            </div>
        </div>
        
        <div class="card">
            <div class="card-header">
                <span class="card-title">
                    <span>🔍</span>
                    SEO Ayarları
                </span>
            </div>
            
            <div class="form-group">
                <label class="form-label">Meta Açıklama</label>
                <textarea name="MetaDescription" class="form-control" rows="3" placeholder="Arama motorlarında görünecek açıklama (155-160 karakter ideal)"></textarea>
            </div>
            
            <div class="form-group">
                <label class="form-label">Meta Anahtar Kelimeler</label>
                <input type="text" name="MetaKeywords" class="form-control" placeholder="Virgülle ayırın: nöbet, vardiya, hemşire">
            </div>
            
            <hr style="margin: 24px 0; border: none; border-top: 2px solid var(--border-color);">
            
            <div class="form-group">
                <label class="form-label">CTA Buton Metni</label>
                <input type="text" name="CtaText" class="form-control" placeholder="Örn: Hemen Başla">
            </div>
            
            <div class="form-group">
                <label class="form-label">CTA Buton URL</label>
                <input type="text" name="CtaUrl" class="form-control" placeholder="Örn: /app">
            </div>
        </div>
    </div>
    
    <div class="card">
        <div class="card-header">
            <span class="card-title">
                <span>📖</span>
                Sayfa İçeriği (HTML)
            </span>
        </div>
        
        <div class="form-group" style="margin-bottom: 0;">
            <textarea name="Content" class="form-control" rows="20" placeholder="HTML içerik yazın..." style="font-family: 'SF Mono', Monaco, monospace; font-size: 0.9rem;"></textarea>
            <small style="color: var(--text-muted); font-size: 0.8rem; margin-top: 8px; display: block;">
                💡 HTML etiketleri kullanabilirsiniz: &lt;h2&gt;, &lt;p&gt;, &lt;ul&gt;, &lt;li&gt;, &lt;strong&gt; vb.
            </small>
        </div>
    </div>
    
    <div style="display: flex; gap: 12px; margin-top: 24px;">
        <button type="submit" class="btn btn-primary btn-lg">
            <span>💾</span>
            Kaydet
        </button>
        <a href="/admin/pages" class="btn btn-secondary btn-lg">İptal</a>
    </div>
</form>
